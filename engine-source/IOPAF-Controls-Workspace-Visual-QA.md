# IOPAF Controls Workspace — Visual QA

The populated desktop view now presents a single working surface. Assessment level
and target appear at the top, followed by a compact eight-domain rail, a filtered
control queue and one selected-control workspace. The Oracle Finance Database D3-05
example keeps the compliance question, implementation, L0–L5 scale, evidence
sample, posterior failure, requirement detail, tester note, remediation and
cross-target comparison visible without navigating away.

The mobile layout has zero horizontal overflow. Header actions and navigation wrap,
the domain rail becomes a two-column selector, and assessment/evidence/requirement
panels stack in a readable sequence. The long control queue remains scrollable and
all inputs remain reachable. The full register is retained as an alternate view and
its rows and rating scales remain present.

Automated validation confirms clean and populated states, 148-control data
preservation, actual scoped writes through visible controls, prior-only Bayesian
availability, exports, local persistence, no duplicate IDs, no page/console errors,
and zero desktop or mobile horizontal overflow.

The independent visual review asked for the Assurance Graph language to become the
page's organizing system rather than an occasional motif. The final pass therefore
adds a connected decision target → control source → test evidence → failure
posterior → decision/action trace above the assessment fields, removes the app-like
`C` badge, strengthens the navy institutional frame, keeps teal for assurance and
red for risk, and replaces generic labels with evidence-ledger language. The final
desktop review shows the current target, selected control, evidence state, posterior
and accountable next action in one visual reading path.

## Readability correction

The enlarged workspace now meets the defined minimums at 100% zoom: 11 px domain
and control statements, 13.5 px compliance questions, 11 px form values, 10.8 px
guidance text, 10.5 px trace values, 8 px trace labels and 9.8 px comparison rows.
Controls, evidence fields and trace nodes reflow instead of shrinking. Automated
checks report zero horizontal overflow at 1440 px, 1366 px and 390 px. The first
viewport-only screenshot inherited the scroll position from the preceding
interaction; it will be recaptured from the page top for final review.

The corrected top-of-page captures confirm that the workspace is readable at both
1440×1000 and 1366×768 without browser zoom. The target name, selected control,
compliance question and evidence/posterior state carry the strongest hierarchy;
domain names and queue statements remain legible; the assurance trace wraps its
values rather than replacing them with ellipses. The laptop view retains the full
three-column working context above the fold and reports zero horizontal overflow.

The readability review confirmed the stronger font hierarchy and recommended that
the assurance path become less cramped. Its five small nodes were therefore
consolidated into three larger nodes: target/control, evidence/posterior and
decision/action. Trace values now render at 11.5 px and labels at 8.5 px, with
complete wrapped text rather than ellipses. Final validation again passed all logic,
keyboard, scoped-write, export and Bayesian checks, with zero horizontal overflow
at desktop, laptop and mobile widths.

## Structured remediation workspace review

The first 1440-pixel populated capture confirmed that the new ledger preserved two
separate target-specific actions and exposed the description, owner, due date,
priority, status, completion evidence, duplicate, delete and completion controls.
It also revealed that retaining the former two-column tester-note/remediation row
left the action cards narrower than intended. The corrective layout now stacks the
tester note and remediation ledger at the full assessment-column width, and uses a
two-column metadata grid rather than four compressed columns.

The 390-pixel mobile capture confirmed a clean single-column reading order. Action
headers, lifecycle buttons, multiline descriptions, owner, date, priority, status
and completion evidence remain reachable without page-level horizontal overflow.
The full browser workflow validates two-action creation, target isolation,
duplicate/delete/complete operations, local persistence and reload, the Results
action register, Reports download entry, one-row-per-action CSV export, and schema
v9 JSON backup/import restoration.

The final regenerated 1440-pixel capture confirms the correction: the tester note
and action ledger now stack at the full assessment-column width; multiline action
descriptions and completion evidence use visibly larger writing areas; owner and due
date form one row while priority and status form a second; and card actions remain
fully visible. The final 390-pixel capture preserves the same information hierarchy
as one continuous column. Both automated overflow checks remain at zero, and the
browser run completed with no page or console errors.
